import {Text} from 'react-native';
import {ScreenLayout} from "@/components";

export default function ChatScreen() {
    return (
        <ScreenLayout>
            <Text>Chat</Text>
        </ScreenLayout>
    );
}
